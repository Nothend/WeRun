# server/local —— 本地专用目录（不进仓库）

这个目录下除本 README 外的所有文件都被 `.gitignore` 忽略，**不会上传到 GitHub**。

用途示例：
- 真实样例截图（含位置信息，不宜入库）
- 真实配置备份（如 `config.js` / `project.config.json` 的真值副本）
- 临时草稿、调试脚本、生产库副本分析等

放心往里丢本地东西即可。
