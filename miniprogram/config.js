// 发布前需手动修改以下两项，与 server/.env 保持一致：
//   baseUrl         → 与 PUBLIC_BASE_URL 相同
//   notifyTemplateId → 与 WECHAT_NOTIFY_TEMPLATE_ID 相同（不用通知则留空）
module.exports = {
  baseUrl: 'https://your.domain.com',
  notifyTemplateId: '',
};
